import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  editTaskContainer: {
    flex: 1,
    paddingTop: 64
  },
  editTextInput: {
    height: 40,
    borderColor: 'gray',
    borderWidth: 1,
    margin: 10
  }
})

export default styles;
