import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    flex: 1
  },
  textInput: {
    borderColor: 'gray',
    borderWidth: 1,
    height: 40
  }
});

export default styles;
